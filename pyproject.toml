# Analyzers for scripts/ (the screenshot renderer). Go analysis lives in
# staticcheck.conf and the Makefile.

[tool.black]
line-length = 88
target-version = ["py310"]

[tool.ruff]
target-version = "py310"
line-length = 88
src = ["scripts"]
required-version = ">=0.16.4"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "COM812",  # black owns trailing commas
]

[tool.ruff.lint.per-file-ignores]
# screenshot.py is a CLI renderer: function names are the docs, os.path is
# the I/O style, argv slots and SGR widths are the format, and it prints the
# PNG path. New Python files do not inherit these.
"scripts/screenshot.py" = [
    "D103",
    "PTH",
    "PLR2004",
    "T201",
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.10"
strict = true
files = ["scripts"]
# Additional checks not covered by strict=.
warn_unreachable = true
disallow_any_expr = true
disallow_any_decorated = true
disallow_any_explicit = true
disallow_any_unimported = true
local_partial_types = true
strict_bytes = true
enable_error_code = [
    "deprecated",
    "exhaustive-match",
    "explicit-override",
    "ignore-without-code",
    "mutable-override",
    "possibly-undefined",
    "redundant-expr",
    "redundant-self",
    "truthy-bool",
    "truthy-iterable",
    "typeddict-unknown-key",
    "unimported-reveal",
    "unused-awaitable",
    "unused-ignore",
    "used-before-def",
]
